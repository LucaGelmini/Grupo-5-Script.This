<!DOCTYPE html>
<html lang="en">
    <head>
        <%-include("./partials/head",{title:'Login'})%>
    </head>
    <body>
        <header class="header-login">
            <%-include("./partials/header")%>
        </header>
        <main class="main-login">
            <form class="formulario-login" action="/estatus/crear" method="POST" name= "formulario-registro">
                
                <input class="input-texto" type="text" name="unidad" placeholder="Ingrese nuevo estatus" value="<%= locals.oldData && oldData.unidad ? oldData.unidad : null %>">
                <% if(locals.errors && errors.unidad) { %>
                 
                    <ul>
                        <li style="color:red; font-size: 24px; "><%= errors.unidad.msg%>!</li>
                    </ul>
                    
                <%}%>
                <ul class="listaErrores"></ul>

                <button class="boton-login" type="submit">Enviar</button>         
                

            </form>
        </main>
        <%-include('./partials/footer')  %> 
        <script src="/js/secondaryTablesValidation.js"></script>

    </body>
</html>
<!DOCTYPE html>
<html lang="en">
    <head>
        <%-include("./partials/head",{title:'Login'})%>
    </head>
    <body>
        <header class="header-login">
            <%-include("./partials/header")%>
        </header>
        <main class="main-units-list">
           
            <ul class="main-container-units-list">
                <% for(let i=0; i< unidades.length;i++){%>
                    <%if(unidades[i].dataValues.type){%>
                    <div class="text-button-container">
                    <li><%=unidades[i].dataValues.type%></li>
                    <div class="buttons-container">
                    <form action="/units/editar/<%=unidades[i].dataValues.id%>" method="GET" >
                        <button type="submit" class="forms-buttons">Editar</button>

                    </form>
                    <form action="/units/eliminar/<%=unidades[i].dataValues.type%>" method="POST" >
                        <button type="submit" class="forms-buttons">Eliminar</button>

                    </form>
                </div>
                </div>
                    <%}%>
                <%}%>
                <form action="/units/crear" method="GET"><button type="submit" class="forms-buttons">Crear</button></form>
            </ul>
           
        </main>
        <%-include('./partials/footer')  %> 
    </body>
</html>